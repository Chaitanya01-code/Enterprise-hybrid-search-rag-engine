# Backend API base URL (no trailing slash).
# Used by Vite at build time — set this in docker-compose.yml build args
# or as a real .env file for local development.
VITE_BACKEND_URL=http://localhost:8000
